const express = require("express");
const router = express.Router();
const UserData = require("../models/userData.js");

// GET username, pet health, food log

// POST username, pet health, foodlog
module.exports = router;
